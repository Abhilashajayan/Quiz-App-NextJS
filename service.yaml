# service.yaml
apiVersion: v1
kind: Service
metadata:
  name: quiz-app-service
spec:
  selector:
    app: quiz-app
  ports:
    - protocol: TCP
      port: 3
      targetPort: 3001
  type: LoadBalancer
